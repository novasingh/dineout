# Project Modules — Restaurant Management System

This document lists the project's high-level modules and their responsibilities (derived from source files under `app/src/main/java/com/dineout/code`).

**App module**: Android application containing all feature packages and resources.

- **admin**: Management and configuration UI for administrators
  - AddEmployeeActivity, AddTableActivity, AddTabletActivity, AddMenuItemActivity — create/update employees, tables, tablets, menu items
  - AdminPanelActivity, HomeActivity — admin dashboard and entry points
  - IngredientsList, IngredientsListAdapter, IngredientRow — ingredient inventory UI
  - Notifications, NotificationsAdapter, NotificationClass — admin notifications
  - LoginActivity — admin authentication

- **billing**: Payment and billing flows
  - OrderBill, ConfirmPayment, PendingPayments — generate and confirm bills, view pending payments
  - BillAdapter, OrdersAdapter — adapters for billing/order lists
  - DishPrice, DishOrder — price and dish order models
  - Feedback — customer feedback handling

- **hall**: Front-of-house / waiter-side features
  - TableSetup, Tables, Tablets — table and tablet management
  - ReadytoServeOrders, ViewCompletedOrders, ReceivePayment — track orders and process payments in hall
  - FireBaseHelper (DB) — Firebase helper for hall data
  - Adapter, DishAdapter — UI adapters
  - Tracking, OnSwipeTouchListener, ManagerInterface — utilities for interactions and tracking

- **kitchen**: Kitchen staff interface and order handling
  - MainActivity (kitchen), CooksListActivity, AttendanceActivity — kitchen entry, cook list, attendance
  - RecyclerViewAdapterOrdersOfCook, RecyclerViewAdapterCookList — adapters for cooks and orders
  - OrderViewHolder, models/ — order models and view-holders
  - listener/ — event listeners for order updates

- **order**: Customer ordering and cart flows
  - MainActivity (customer), WelcomePage — app entry for customers
  - Menu, MenuItem, Item, Inventory — menu and inventory models
  - cart, cartListView, setQuantity, quantityInvalidPrompt — cart handling and quantity UI
  - confirmOrder, Order, OrderDetails, Time, NotificationClass — placing orders, order details, notifications

- **reporting**: Reports, email/notification utilities, end-of-day processing
  - EndOfDay_EventHandler — end-of-day report handling
  - GMailSender, EmailSender/ — email sending utilities for reports/notifications
  - JSSEProvider — security provider helper used by mail sending
  - order (reporting) — reporting model for orders

Other project items:
- `app/build.gradle`, top-level Gradle files — build configuration for Android app
- `google-services.json` — Firebase configuration present in `app/`
- `database/` — includes a Firebase Realtime DB backup JSON file

Notes / next steps:
- Review each package's source files for more detailed descriptions (methods, intents, data flows).
- If you want, I can expand each module section to include class-level responsibilities and key public methods.

Generated by automated project scan.
